define serverHandler as abstract method receiving Document doc

define installServerHandler as native method receiving Text path and serverHandler doing:
    Java: $server.installHandler(path, serverHandler);

define customHandler as method receiving Document doc doing:
	print "received!"
	
define serverAboutToStart as method receiving Text{} args doing:
	installServerHandler with "/test/*" as path and customHandler as serverHandler
